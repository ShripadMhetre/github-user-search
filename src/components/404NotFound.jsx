import React from "react";

const NotFound = () => (
  <div className="container text-center mt-5">
    <h2 className="text-danger">Error 404: Route doesn't exist.</h2>
  </div>
);

export default NotFound;
